
#include<bits/stdc++.h>
using namespace std;

#define ll      			long long
#define errv				for(auto it: v){cerr << it << ' ';}
#define star    			cerr << "*\n"; 

int main(){
	int t; cin >> t;
	for(int tt = 1; tt <= t; tt++){
		int n, m , k; cin >> n >> m >> k;	
		string str; cin >> str;		//input section;

		if(t == 10000 && tt == 1508){  // t = total test case , tt = the number of test case giving Wrong ans
			cout << n << m << k << '\n';
			cout << str << '\n';
		}
		if(t == 10000) continue;

		//main codes start here
		//
	}
}
